<?php

    // $name = "kishan.panchal";
    $name;
    // echo $name;  

    echo isset($name) ? "variable is set" : "variable is not set ";
    

    // example 

    $name2 = "kishan";
    $pre = $name2;

    echo isset($pre) ? "display  " . $pre : "variable is not set";